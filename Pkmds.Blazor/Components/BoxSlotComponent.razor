<td>
    <div class="box-slot">
        @if (Pokemon is { Species: > 0 })
        {
            @*<img class="box-sprite" src="@spriteImageUrl" alt="@SpeciesName.GetSpeciesName(Pokemon.Species, (int)LanguageID.English)" />*@
            <span class="pokesprite pokemon @SpeciesName.GetSpeciesName(Pokemon.Species, (int)LanguageID.English).ToLower().Replace(" ", "").Replace(".", "-")"></span>
        }
    </div>
</td>
