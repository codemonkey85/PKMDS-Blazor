@inherits BasePkmdsComponent

<MudDialog>
    <TitleContent>
        <MudText Typo="@Typo.h6">Alpha Mastered Editor</MudText>
    </TitleContent>
    <DialogContent>
        @if (Pokemon is IMoveShop8Mastery)
        {
            <MudDataGrid T="@AlphaMasteredInfo"
                         Items="@AlphaMasteredMoves"
                         Hover
                         Dense
                         ReadOnly
                         Height="500px"
                         FixedHeader>
                <Columns>
                    <TemplateColumn Title="" Sortable="false">
                        <CellTemplate>
                            <MudCheckBox T="@bool"
                                         Value="@context.Item.IsMastered"
                                         ValueChanged="@(value => ToggleAlphaMasteredFlag(context.Item.Index, value))"
                                         Dense />
                        </CellTemplate>
                    </TemplateColumn>
                    <PropertyColumn Property="@(x => x.Index)" Title="#" />
                    <TemplateColumn Title="">
                        <CellTemplate>
                            @MoveTypeSummary(context.Item.Type)
                        </CellTemplate>
                    </TemplateColumn>
                    <PropertyColumn Property="@(x => x.Name)" Title="Move" />
                </Columns>
            </MudDataGrid>
        }
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="@GiveAll"
                   Variant="@Variant.Outlined"
                   Color="@Color.Primary">
            Give All
        </MudButton>
        <MudButton OnClick="@RemoveAll"
                   Variant="@Variant.Outlined"
                   Color="@Color.Secondary">
            Remove All
        </MudButton>
        <MudSpacer />
        <MudButton OnClick="@Cancel"
                   Variant="@Variant.Outlined">
            Cancel
        </MudButton>
        <MudButton OnClick="@Save"
                   Variant="@Variant.Filled"
                   Color="@Color.Primary">
            Save
        </MudButton>
    </DialogActions>
</MudDialog>
