@inherits BasePkmdsComponent

<MudDialog>
    <TitleContent>
        <MudText Typo="@Typo.h6">Fashion Editor</MudText>
    </TitleContent>
    <DialogContent>
        <MudTabs Elevation="0"
                 ActivePanelIndex="@_activePanelIndex"
                 ActivePanelIndexChanged="@OnActivePanelChanged">
            @if (SaveFile is SAV9SV)
            {
                @foreach (var section in _svSections)
                {
                    <MudTabPanel Text="@section.Label">
                        @if (section.Items is null)
                        {
                            @LoadingPlaceholder
                        }
                        else
                        {
                            <MudDataGrid T="@FashionItemModel"
                                         Items="@section.Items"
                                         Hover
                                         Dense
                                         ReadOnly
                                         Height="450px"
                                         FixedHeader>
                                <Columns>
                                    <PropertyColumn Property="@(x => x.Index)"
                                                    Title="#"/>
                                    <TemplateColumn Title="ID">
                                        <CellTemplate>
                                            @(context.Item.IsEmpty
                                                ? "—"
                                                : context.Item.Value.ToString())
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="New">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsNew"
                                                         ValueChanged="@(v => context.Item.IsNew = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                </Columns>
                            </MudDataGrid>
                        }
                    </MudTabPanel>
                }
            }
            else if (SaveFile is SAV9ZA)
            {
                @foreach (var section in _zaClothingSections)
                {
                    var clothingSection = section;
                    <MudTabPanel Text="@clothingSection.Label">
                        @if (clothingSection.Items is null)
                        {
                            @LoadingPlaceholder
                        }
                        else
                        {
                            <div class="d-flex align-center gap-2 mb-2">
                                <MudButton Size="@Size.Small"
                                           Variant="@Variant.Filled"
                                           Color="@Color.Primary"
                                           OnClick="@(() => { SetAllOwned(clothingSection.Items); StateHasChanged(); })">
                                    Set All Owned
                                </MudButton>
                            </div>
                            <MudDataGrid T="@FashionItem9aModel"
                                         Items="@clothingSection.Items"
                                         Hover
                                         Dense
                                         ReadOnly
                                         Height="400px"
                                         FixedHeader>
                                <Columns>
                                    <PropertyColumn Property="@(x => x.Index)"
                                                    Title="#"/>
                                    <TemplateColumn Title="ID">
                                        <CellTemplate>
                                            @(context.Item.IsEmpty
                                                ? "—"
                                                : context.Item.Value.ToString())
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="New">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsNew"
                                                         ValueChanged="@(v => context.Item.IsNew = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="New (Shop)">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsNewShop"
                                                         ValueChanged="@(v => context.Item.IsNewShop = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="New (Group)">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsNewGroup"
                                                         ValueChanged="@(v => context.Item.IsNewGroup = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="Equipped">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsEquipped"
                                                         ValueChanged="@(v => context.Item.IsEquipped = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="Owned">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsOwned"
                                                         ValueChanged="@(v => context.Item.IsOwned = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                </Columns>
                            </MudDataGrid>
                        }
                    </MudTabPanel>
                }
                @foreach (var section in _zaHairSections)
                {
                    <MudTabPanel Text="@section.Label">
                        @if (section.Items is null)
                        {
                            @LoadingPlaceholder
                        }
                        else
                        {
                            <MudDataGrid T="@FashionItemModel"
                                         Items="@section.Items"
                                         Hover
                                         Dense
                                         ReadOnly
                                         Height="450px"
                                         FixedHeader>
                                <Columns>
                                    <PropertyColumn Property="@(x => x.Index)"
                                                    Title="#"/>
                                    <TemplateColumn Title="ID">
                                        <CellTemplate>
                                            @(context.Item.IsEmpty
                                                ? "—"
                                                : context.Item.Value.ToString())
                                        </CellTemplate>
                                    </TemplateColumn>
                                    <TemplateColumn Title="New">
                                        <CellTemplate>
                                            <MudCheckBox T="@bool"
                                                         Value="@context.Item.IsNew"
                                                         ValueChanged="@(v => context.Item.IsNew = v)"
                                                         Disabled="@context.Item.IsEmpty"
                                                         Dense/>
                                        </CellTemplate>
                                    </TemplateColumn>
                                </Columns>
                            </MudDataGrid>
                        }
                    </MudTabPanel>
                }
            }
        </MudTabs>
    </DialogContent>
    <DialogActions>
        @if (SaveFile is SAV9ZA)
        {
            <MudButton Variant="@Variant.Filled"
                       Color="@Color.Primary"
                       OnClick="@SetAllOwnedAllTabs">
                Set All Owned (All Tabs)
            </MudButton>
        }
        <MudSpacer/>
        <MudButton OnClick="@Cancel"
                   Variant="@Variant.Filled"
                   Color="@Color.Default">
            Cancel
        </MudButton>
        <MudButton OnClick="@Save"
                   Variant="@Variant.Filled"
                   Color="@Color.Primary">
            Save
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    private static readonly RenderFragment LoadingPlaceholder = @<text>
        <div class="d-flex flex-column justify-center align-center"
             style="height:450px; gap:1rem;">
            <MudProgressCircular Indeterminate
                                 Color="@Color.Primary"
                                 Size="@Size.Large"/>
            <MudText Typo="@Typo.body1">Loading…</MudText>
        </div>
    </text>;
}
